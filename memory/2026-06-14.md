# 2026-06-14

- Simplified `demo_gui.py` so the Dashboard tab displays only Live State telemetry, inference, confidence, stability, and its explanatory note.
- Removed the Configuration tab's Config File/Coverage/Validation summary cards and Config Workflow help panel.
- Removed the Diagnostics tab while retaining bounded in-memory runtime logging.
- Embedded Diagnostics beneath Live State in the Dashboard tab while keeping Controls separate.
- Moved Controls into Dashboard and made Diagnostics an optional tab enabled by `PERFANALYZE_DEBUG=1` or `run_demo_gui.ps1 -Debug`.
- Added a modular `Power Plans` tab backed by `PowerPlanEditorState` and `PowerPlansEditor`, with an unlimited scrollable plan rail, draft add/remove, catalog persistence, multidimensional intent fields, AC/battery setting entries, mapping summaries, and read-only support discovery.
- The Power Plans editor only writes `power_plan_catalog.json`; it never mutates or activates Windows schemes.
- Audited the Power Plans GUI against `POWER_PLANS_BACKEND.md`: `plan_id` and `windows_guid` are preserved from the selected `PlanDefinition`, the GUID is read-only, mapped-plan removal requires confirmation, and discovered option/range metadata validates values without hardcoded setting ranges.
- Removed the visible legacy Configuration tab and its threshold, hotkey, legacy mapping, preset, and config-file action widgets. The existing runtime config still loads internally for controller compatibility.
- Kept Controls, Configuration, and the underlying runtime state updates unchanged.
- Verified GUI construction with the project virtual environment and ran the passing unit test suite.
- Added Windows GPU telemetry features using background performance-counter sampling.
- Changed recording to create one CSV per labeled session under `data/<label>/`.
- Changed training to recursively combine session CSVs, split by whole sessions, train fresh weights, save versioned/latest artifacts, and print only final test accuracy.
- Verified the collection-to-training workflow and all 78 tests.
- Added a shared Google-inspired light theme with rounded cards/buttons, soft gray surfaces, blue accents, and matching dashboard/power-plan editor styling.
- Visually checked both GUI tabs and verified normal/debug construction plus all 78 tests.
- Added `power_plan_cli.py` as a backend-only interface for catalog inspection, read-only Windows discovery, and dry-run managed-plan command previews.
- Added ownership-gated managed activation plus CLI/service tests; all 70 project tests pass.
- Live read-only validation found all five portable controls and validated every default plan with zero warnings. The active `Battery - Optimized` scheme remained unchanged.
